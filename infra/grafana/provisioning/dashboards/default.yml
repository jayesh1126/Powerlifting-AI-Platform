apiVersion: 1
providers:
  - name: default
    type: file
    options:
      path: /var/lib/grafana/dashboards
    # Let dashboards live in nested folders under that path later.
    foldersFromFilesStructure: true
