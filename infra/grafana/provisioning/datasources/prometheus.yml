apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus        # pinned so exported dashboard JSON works on any instance
    type: prometheus
    access: proxy          # Grafana's backend queries Prometheus, not browser
    url: http://prometheus:9090
    isDefault: true
