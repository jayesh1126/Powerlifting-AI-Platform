# Shared secret — must match ORCHESTRATOR_API_KEY in web/.env
INTERNAL_API_KEY=dev-secret-change-me

# LLM providers (wire up when porting the real pipelines)
# OPENROUTER_API_KEY=
# OPENAI_API_KEY=

# Data stores for RAG (wire up when porting the real pipelines)
# SUPABASE_URL=
# SUPABASE_SECRET_KEY=
# NEO4J_URI=
# NEO4J_USER=
# NEO4J_PASSWORD=
# POSTGRES_DSN=
