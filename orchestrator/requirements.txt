fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

# AI runtime
openai>=1.55          # OpenAI-compatible client (used against OpenRouter too)
httpx>=0.27           # Supabase PostgREST RPC calls (hybrid vector search)
asyncpg>=0.29         # read-only OpenPowerlifting Postgres
langgraph>=1.2        # runtime graph orchestration (app/runtime/graph.py)

# Observability
prometheus-client>=0.21

# Later:
# deepeval / ragas     # evaluation
